enum UserRoles {
    ADMIN = 'admin',
    SUBSCRIBER = 'subscriber'
};

enum UserStatuses {
    ACTIVE = 'active',
    PENDING = 'pending',
    SUSPEND = 'suspend',
    DELETED = 'deleted',
};

export {UserRoles, UserStatuses};